### Local environment variables (example)
### Copy to `.env` (do NOT commit `.env`)

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: Appwrite Cloud (AC) if used
APPWRITE_ENDPOINT=
APPWRITE_PROJECT_ID=
APPWRITE_API_KEY=

